/**
 * Dimensions export flyer — alignées avec `frontend/src/features/app-flyer-qr-presets.js` (`FLYER_EXPORT`).
 * Ratio 2:3, ultra qualité impression (~300 DPI sur A2).
 */
export const FLYER_EXPORT_WIDTH = 4096;
export const FLYER_EXPORT_HEIGHT = 6144;
