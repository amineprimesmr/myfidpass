{
  "name": "fidelity-apple-wallet",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "backend": "node backend/src/index.js",
    "frontend": "npm run dev --prefix frontend",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "build:frontend": "cd frontend && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
